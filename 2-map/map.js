/* MAP */

    // Creates an array of values by running each element in collection thru callback function. 
    // The callback function is invoked with three arguments:
    // (value, index|key, collection).

/* ARGUMENTS */

    // collection (Array|Object): The collection to iterate over.
    // callback (Function): The function invoked per iteration.

/* RETURN */

    // (Array): Returns the new mapped array.

import {forEach} from "../1-forEach/forEach.js";

export function map(collection, callback) {
    // CODE GOES HERE
}